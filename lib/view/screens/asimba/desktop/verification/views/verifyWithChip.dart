import 'package:flutter/material.dart';

class verifyWithChip extends StatelessWidget {
  const verifyWithChip({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}